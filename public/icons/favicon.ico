<!-- This is a placeholder. In a real project, you would convert the SVG to ICO format using a tool. 
     Since we can't directly create binary ICO files here, you would need to use a conversion tool offline. -->
